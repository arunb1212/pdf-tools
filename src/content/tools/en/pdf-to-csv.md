---
tool: "pdf-to-csv"
locale: "en"
component: "PdfToCsv"
processing: "hybrid"
title: "PDF to CSV — extract tables from PDFs into CSV, free"
shortTitle: "PDF to CSV"
description: "Extract tabular data from a PDF into a CSV file. Simple tables are read entirely in your browser; complex or scanned tables use a server fallback that never stores your file."
steps:
  - "Add the PDF that contains the table you want."
  - "Wait while the text layer is read (or OCR is applied if there's no text)."
  - "Click Download to save the extracted data as a .csv file."
faq:
  - q: "How does table extraction work?"
    a: "For most PDFs with a text layer, the rows and columns are read locally in your browser. If the layout is too complex, we clearly label when a server fallback is used."
  - q: "Are my files stored?"
    a: "No. Any file processed server-side is deleted immediately after the result is returned. Simple extractions never leave your device."
  - q: "What about scanned PDFs with no text?"
    a: "If there's no text layer, the server fallback applies OCR to reconstruct the table."
---

# PDF to CSV

Extract tables out of PDFs into a spreadsheet-friendly CSV — mostly right in your browser.
